#trigger:
#- master

pool:
  vmImage: 'ubuntu-latest'

steps:
#- task: NodeTool@0
#  inputs:
#    versionSpec: '10.x'
#  displayName: 'install node.js'

#- script: |
#    npm install
#    npm run build
#  displayName: 'npm install and build'

- script: |
    git config user.name "Mischa Skyda"
    git config user.email "mskyda@gmail.com"
    git pull origin master
    touch test.txt
    git add .
    git commit -m "test"
    git config gc.auto 0
    git config --get-all http.https://github.com/mskyda/db-frontend.extraheader
    git config --get-all http.proxy
    git -c http.extraheader="AUTHORIZATION: basic ***" push
  displayName: 'push the production bundle'